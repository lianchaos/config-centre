scegcms:
  user: lianchao
  mail: 245073271@qq.com
  sex: 男
  url: www.lianchao.lch
  explain: dev 测试环境配置文件

# 环境配置
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
      
#info 信息
info:
  app:
    #从pom.xml中获取
    name: @project.name@
    version: "@project.version@"
    spring-boot-version: "@project.parent.version@"
    groupId: "@project.groupId@"
    artifactId: "@project.artifactId@"
    packaging: "@project.packaging@"
    description: "@project.description@"
    port: ${server.port}
    url: ${eureka.instance.hostname}:${server.port}      
      
      
      
      
